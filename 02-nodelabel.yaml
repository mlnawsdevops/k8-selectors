apiVersion: v1 
kind: Pod 
metadata:
  name: nginx
spec:
  containers:
  - name: nginx
    image: nginx
  tolerations:
  - key: "hardware"
    value: "gpu"
    operator: "Equal"
    effect: "NoSchedule"
  nodeSelector: # label node 
    hardware: gpu